Place sample timelapse video and music track here for local testing.

Expected filenames (referenced by `config/job.example.json`):

- `timelapse.mp4` — source video
- `track.mp3` — background music (defines output duration)

Media files are gitignored; only this README is tracked.
